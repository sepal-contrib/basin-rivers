# default packages to run the app
voila
earthengine-api


# This version contains file input improements and a fix for the map resize
git+https://github.com/12rambau/sepal_ui.git@sepal_pre_release

seaborn
plotly